{
    "development": {
      "DATABASE_URI": "{{ DEV_DB_URI }}",
      "SECRET_KEY": "{{ DEV_DB_SECRET_KEY }}"
    },
    "testing": {
      "DATABASE_URI": "{{ TEST_DB_URI }}",
      "SECRET_KEY": "{{ TEST_DB_SECRET_KEY }}"
    }
  }
  